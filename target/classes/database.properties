# Database Configuration
db.url=jdbc:mysql://localhost:3306/inventory_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.username=root
db.password=Sai@7804

# Connection Pool Settings
db.pool.maxSize=10
db.pool.minIdle=2

# Alert Settings
alerts.lowStock.threshold=10
alerts.expiry.days=7

# Application Settings
app.name=Smart Inventory Management System
app.version=1.0.0
